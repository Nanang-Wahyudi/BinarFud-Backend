# Important configuration for JPA
spring.datasource.url=jdbc:postgresql://${database.postgres.url}/${database.postgres.name}
spring.datasource.username=${database.postgres.username}
spring.datasource.password=${database.postgres.password}
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
# Optional configuration for JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.jpa.open-in-view=false
spring.datasource.hikari.auto-commit=false

#For JWT
jwt.secret=km5bej1
jwt.expiration.ms=18000000

#For Swagger Authorization
module-name=BinarFud-Backend
api-version=1.0.0

#For Firebase
app.firebase-configuration-file=binarfud-329b5-firebase-adminsdk-a2xjq-6bedeada5b.json

#For SMTP Gmail
email.account={email_account}
email.password={email_password}
email.host=smtp.gmail.com
email.port=587
email.auth=true
email.tls=true


# Link API Documentation : http://localhost:8080/swagger-ui/index.html#/

